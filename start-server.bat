@echo off
echo Starting local server on http://localhost:3000
echo Press Ctrl+C to stop
powershell -Command "& {Add-Type -AssemblyName System.Web; $listener = New-Object System.Net.HttpListener; $listener.Prefixes.Add('http://localhost:3000/'); $listener.Start(); Write-Host 'Server running at http://localhost:3000'; Write-Host 'Open: http://localhost:3000'; start 'http://localhost:3000'; while ($listener.IsListening) { try { $context = $listener.GetContext(); $request = $context.Request; $response = $context.Response; $localPath = $request.Url.LocalPath; if ($localPath -eq '/') { $localPath = '/index.html' }; $filePath = Join-Path (Get-Location) $localPath.TrimStart('/'); if (Test-Path $filePath) { $ext = [System.IO.Path]::GetExtension($filePath); switch ($ext) { '.html' { $response.ContentType = 'text/html' } '.css' { $response.ContentType = 'text/css' } '.js' { $response.ContentType = 'application/javascript' } '.png' { $response.ContentType = 'image/png' } '.jpg' { $response.ContentType = 'image/jpeg' } '.jpeg' { $response.ContentType = 'image/jpeg' } }; $content = [System.IO.File]::ReadAllBytes($filePath); $response.ContentLength64 = $content.Length; $response.OutputStream.Write($content, 0, $content.Length) } else { $response.StatusCode = 404; $errorContent = [System.Text.Encoding]::UTF8.GetBytes('404 - File Not Found'); $response.OutputStream.Write($errorContent, 0, $errorContent.Length) }; $response.Close() } catch { Write-Host 'Error: ' $_.Exception.Message } } }"
